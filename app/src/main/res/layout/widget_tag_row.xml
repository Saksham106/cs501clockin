<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingBottom="3dp">

    <FrameLayout
        android:id="@+id/tag_slot_left_container"
        android:layout_width="0dp"
        android:layout_height="@dimen/widget_chip_height"
        android:layout_marginEnd="4dp"
        android:layout_weight="1"
        android:visibility="gone">

        <ImageView
            android:id="@+id/tag_row_left_bg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="fitXY" />

        <TextView
            android:id="@+id/tag_row_left"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:clickable="true"
            android:focusable="true"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:paddingLeft="6dp"
            android:paddingRight="6dp"
            android:textSize="11sp" />
    </FrameLayout>

    <FrameLayout
        android:id="@+id/tag_slot_right_container"
        android:layout_width="0dp"
        android:layout_height="@dimen/widget_chip_height"
        android:layout_weight="1"
        android:visibility="gone">

        <ImageView
            android:id="@+id/tag_row_right_bg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="fitXY" />

        <TextView
            android:id="@+id/tag_row_right"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:clickable="true"
            android:focusable="true"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:paddingLeft="6dp"
            android:paddingRight="6dp"
            android:textSize="11sp" />
    </FrameLayout>
</LinearLayout>
